from airflow.models import DagBag

def test_task():
    "To check task count of the dag"
    # dag_id = "download_ches_codebook"
    # dagbag = DagBag()
    # dag = dagbag.get_dag(dag_id)
    # print(dag.tasks)
    pass